{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Add a new CIDR address to the IP allowlist",
  "properties": {
    "parameters": {
      "description": "struct for passing parameters to the method [`cockroach_cloud_add_allowlist_entry`]",
      "properties": {
        "allowlist_entry": {
          "$ref": "#/definitions/AllowlistEntry"
        },
        "cluster_id": {
          "type": "string"
        }
      },
      "required": [
        "allowlist_entry",
        "cluster_id"
      ],
      "title": "CockroachCloudAddAllowlistEntryParams",
      "type": "object"
    }
  },
  "title": "cockroachdb_cockroach_cloud_add_allowlist_entry",
  "type": "object"
}